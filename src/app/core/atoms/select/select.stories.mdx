import {Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import {SelectComponent} from "./select.component"; import {MatSelectModule} from "@angular/material/select"; import {ReactiveFormsModule} from "@angular/forms"; import {IconsModule} from "../icons/icons.module"; import {BrowserAnimationsModule} from "@angular/platform-browser/animations"; import {IconsRegistryModule} from "../icons/icons-registry.module";

<Meta
    title="Atoms/Select"
    component={SelectComponent}
    parameters={{layout: 'padded'}}/>

# Select
<br/>
<br/>

### Props
| Property | Type  | Values | Default  |
| ------ | --------- |------ | --------- |
| optionList    | any[]   |  | |
| value    | any   |     ""    |  ""  |
| clearValue    | string   |     ""    |  ""  |
| placeholder    | string   |  | "placeholder" |
| disabled      | boolean  | true/false       | false     |

### Events
| Event | Arguments  | Description |
| ------ | --------- |------ |
| optionSelected    |     | Emitted when select's value changes |
| change    |     | Emitted when select's value changes |

------

export const Template = (args) => ({
    component: SelectComponent,
    props: {
        ...args,
        placeholder: 'placeholder',
    },
    moduleMetadata: {
        imports: [BrowserAnimationsModule, MatSelectModule, ReactiveFormsModule, IconsModule, IconsRegistryModule],
        declarations: [SelectComponent],
    },
    template: '<app-select ' +
        '[placeholder]="placeholder" ' +
        '[value]="value" ' +
        '[disabled]="disabled" ' +
        '[clearValue]="clearValue" ' +
        '[optionList]="optionList"></app-select>',
})

<br/>
<br/>

### Appearance

<Canvas>
    <Story name='Default'
           height={350}
           args={{
               value: 1,
               optionList: [1, 2, 3, 4, 5]
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-select value="1" optionList="[1,2,3,4,5]"></app-select>',
                   }
               }
           }}>{Template.bind({})}
    </Story>
    <Story name='Clear Value'
           height={350}
           args={{
               value: 'red',
               clearValue: 'Select Color',
               optionList: ['blue', 'green', 'red', 'yellow', 'white']
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-select value=c" optionList="[a,b,c,d,e]" clearValue="Select color"></app-select>',
                   },
               }
           }}>{Template.bind({})}
    </Story>
    <Story name='Disabled'
           height={350}
           args={{
               value: 'b',
               disabled: true,
               optionList: ['a', 'b', 'c', 'd', 'e']
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-select value=c" optionList="[a,b,c,d,e]" disabled="true"></app-select>',
                   },
               }
           }}>{Template.bind({})}
    </Story>
</Canvas>

<br/>
<br/>

# Usages

Import InputsModule into your custom module

```typescript
import { SelectModule } from '@atoms/select/select.module';

@NgModule({
    imports: [SelectModule]
})

export class YourModule {}
```

## Examples

  ```html
    <app-select [optionList]="['7:00 PM']" [value]="'7:00 PM'"></app-select>
```
 ```html
    <app-select [optionList]="['8:00 AM']" [value]="'8:00 AM'"></app-select>
  ```
