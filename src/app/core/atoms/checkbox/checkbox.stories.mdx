import {Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import {CheckboxComponent} from "./checkbox.component";

<Meta
    title="Atoms/Checkbox"
    component={CheckboxComponent}/>

# Checkbox

<br/>
<br/>

### Props
| Property | Type  | Values | Default  |
| ------ | --------- |------ | --------- |
| name      | string   | "" | checkbox |
| disabled      | boolean  | true/false       | false     |
| checked   | boolean |   true/false     | false  |
| indeterminate   | boolean |   ""     | ""  |
| innerClass    | string   |     true/false    |  false  |

### Events
| Event | Arguments  | Description |
| ------ | --------- |------ |
| click    |     | Emitted when clicked on checkbox |
| change    |     | Emitted when the checkbox's checked value changes |

------

export const Template = (args) => ({
    component: CheckboxComponent,
    props: {...args, label: 'checkbox'},
    moduleMetadata: {
        declarations: [CheckboxComponent],
    },
    template: '<app-checkbox ' +
        '[disabled]="disabled" ' +
        '[checked]="checked" ' +
        '[indeterminate]="indeterminate" ' +
        '[innerClass]="innerClass" ' +
        '[name]="name">{{label}}</app-checkbox>',
})

<br/>
<br/>

### Appearance

<Canvas>
    <Story name='Default' parameters={{
        docs: {
            source: {
                code: '<app-checkbox>checkbox</app-checkbox>',
            }
        }
    }}>{Template.bind({})}
    </Story>
    <Story name='Checked'
           args={{
               checked: true
           }} parameters={{
        docs: {
            source: {
                code: '<app-checkbox checked="true">checkbox</app-checkbox>',
            }
        }
    }}>{Template.bind({})}
    </Story>
    <Story name='Indeterminate'
           args={{
               indeterminate: true
           }} parameters={{
        docs: {
            source: {
                code: '<app-checkbox indeterminate="true">checkbox</app-checkbox>',
            }
        }
    }}>{Template.bind({})}
    </Story>
    <Story name='Disabled' args={{
        disabled: true
    }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-checkbox disabled="true">checkbox</app-checkbox>',
                   },
               }
           }}>{Template.bind({})}
    </Story>
</Canvas>

<br/>
<br/>


# Usages

Import CheckboxModule into your custom module

```typescript
import { CheckboxModule } from '@atoms/checkbox/checkbox.module';

@NgModule({
    imports: [CheckboxModule]
})

export class YourModule {}
```

## Examples
  ```typescript
  @Component({...})
export class YourCustomComponent {
    checked = false;
    indeterminate = false;
    disabled = false;
}
  ```
  ```html
<app-checkbox [(ngModel)]="checked">Checked</app-checkbox>
<app-checkbox [(ngModel)]="indeterminate">Indeterminate</app-checkbox>

<app-checkbox [checked]="checked"
              [disabled]="disabled"
              [indeterminate]="indeterminate">I'm a checkbox</app-checkbox>
```
