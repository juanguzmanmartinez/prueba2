import {Canvas, Meta, Story} from '@storybook/addon-docs/blocks';
import {TagComponent} from "./tag.component";

<Meta
    title="Atoms/Tag"
    component={TagComponent}/>

# Tag
<br/>
<br/>

### Props
| Property | Type  | Values | Default  |
| ------ | --------- |------ | --------- |
| tagStatus    | string |  <code>error</code><code>warning</code><code>success</code> | |


export const Template = (args) => ({
    component: TagComponent,
    props: {
        ...args,
    },
    moduleMetadata: {
        declarations: [TagComponent],
    },
    template: '<app-tag ' +
        '[tagStatus]="tagStatus">{{content}}</app-tag>',
})

<br/>
<br/>

### Appearance

<Canvas>
    <Story name='Error'
           args={{
               content: 'Error',
               tagStatus: 'error'
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-tag tagStatus="error">Error</app-tag>',
                   }
               }
           }}>{Template.bind({})}
    </Story>
    <Story name='Warning'
           args={{
               content: 'Warning',
               tagStatus: 'warning'
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-tag tagStatus="warning">Warning</app-tag>',
                   }
               }
           }}>{Template.bind({})}
    </Story>
    <Story name='Success'
           args={{
               content: 'Success',
               tagStatus: 'success'
           }}
           parameters={{
               docs: {
                   source: {
                       code: '<app-tag tagStatus="success">Success</app-tag>',
                   },
               }
           }}>{Template.bind({})}
    </Story>
</Canvas>

<br/>
<br/>

# Usages

Import InputsModule into your custom module

```typescript
import { SwitchModule } from '@atoms/switch/switch.module';

@NgModule({
    imports: [SwitchModule]
})

export class YourModule {}
```

## Examples

  ```html
    <app-tag tagStatus="error">Error</app-tag>
```
 ```html
    <app-tag tagStatus="warning">Warning</app-tag>
  ```
