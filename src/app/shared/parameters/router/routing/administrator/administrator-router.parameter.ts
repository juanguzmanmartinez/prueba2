import { TRouter } from '@models/auth/router.model';

export const ADMINISTRATOR_ROUTER: TRouter = {
  path: 'administrador',
  name: 'Administrador',
  clickable: true
};

export const AD_CHILDREN_PATH = {
  users: 'usuarios'
};
