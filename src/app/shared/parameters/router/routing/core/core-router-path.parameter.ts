import { CORE_ROUTER } from '@parameters/router/routing/core/core-router.parameter';

export const CORE_ROUTER_PATH = {
    base: `/${CORE_ROUTER.base.path}`,
    notFound: `/${CORE_ROUTER.notFound.path}`,
    notInternetConnection: `/${CORE_ROUTER.notInternetConnection.path}`,
};
